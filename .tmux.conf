set -g prefix C-x
set -g mode-keys vi
set -g mode-mouse on

unbind '"'
bind - splitw -v 

unbind %
bind | splitw -h
bind k selectp -U
bind j selectp -D
bind h selectp -L
bind l selectp -R

# bind a reload key 
bind r source-file ~/.tmux.conf; display-message "Config reloaded.."
